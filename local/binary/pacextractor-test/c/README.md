### Requirements:

GCC(MinGW-w64 is suggested) or Clang

### Build:

`bash build.sh`

### Usage:

```
pacextractor [-d] [-c] firmware.pac [outdir]
         -d      enable debug output
         -c      compute and verify CRC16
```
